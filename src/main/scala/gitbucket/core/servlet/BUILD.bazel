package(default_visibility = ["//visibility:public"])

scala_library(
    name = "servlet",
    srcs = glob(["*.java"]) + glob(["*.scala"]),
    runtime_deps = [
    ],
    deps = [
        "@scalac_rules_commons_io",
    ],
)
